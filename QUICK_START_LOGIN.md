# Quick Start: Login and Signup

```bash
# Create a user
./expensevault signup -u alice

# Login
./expensevault login -u alice

# Launch TUI after login
./expensevault tui
```

Passwords are hashed with bcrypt and stored in the database.
